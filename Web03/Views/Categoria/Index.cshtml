@section scripts{
    <script src="~/Scripts/categoria/index-ajax.js"></script>
}

<h2 class="text-info text-center mb-3 mt-3">Categorias</h2>
<button type="button" class="float-right btn btn-outline-primary mb-2" data-toggle="modal" data-target="#modal-cadastro-categoria">
    Cadastro Categoria
</button>
<table class="table table-striped table-hover table-bordered">
    <thead>
        <tr>
            <th class="coluna-codigo">Codigo</th>
            <th class="coluna-nome">Nome</th>
            <th>Ação</th>
        </tr>
    </thead>
    <tbody id="tabela-categoria"></tbody>
    <tfoot>
        <tr>
            <th class="coluna-codigo">Codigo</th>
            <th class="coluna-nome">Nome</th>
            <th>Ação</th>
        </tr>
    </tfoot>
</table>
@RenderPage("ModalCadastrar.cshtml")
@RenderPage("ModalEditar.cshtml")